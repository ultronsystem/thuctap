
<h5>Chỉnh sửa bình luận của bạn:</h5>
<%= form_for [@book, @review, @comment], html: {class: "form-horizontal content_cmt"} do |f| %>
  <div class = "control-group">
    <%= f.text_area :content, require: true, cols:100, class: "ta-review cmt" %>
  </div>
  <div class = "control-group">
    <%= f.button "Đăng", class: "shopBtn" %>
  </div>
<% end %>
